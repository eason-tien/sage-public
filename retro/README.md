<!-- AGENT-FACING. 執行後檢討集中地。提醒非強制：不擋任何流程（工具不是監獄）。 -->
# retro — 執行後檢討（30 秒/則）

- 每次 SAGE 執行收尾（verify 真綠後）跑 `sage retro <任務名>` → 生成 `YYYYMMDD-HHMM-<任務名>.md`（UTC，零填充，字典序即時間序）。
- 3 問＋一行「值不值」，30 秒填完；空著也不會擋任何流程——這是提醒與便利，不是閘。
- 每週彙整見 `checklists/weekly-retro.md`；處理游標記在本目錄 `WEEKLY.md`。
- 與任務倉的 `sop/lessons.md` 分工：lessons 留在任務倉（給接手者），retro 集中在這裡（給 SOP 自我改進）。
