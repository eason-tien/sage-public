Re-read WORKFLOW_TASK.md and the current implementation. Validate the reviewer report
against the contract. Fix every valid blocking finding, reject false positives, preserve
tests and the task contract, and run the relevant repository tests before finishing.

